-1 Will I have HPC account? 
0- should I do instruction count analysis to see where is the bottleneck? Should I run the code on CPU? 
1- should I create a new config file as include_NVCC.mk for cuda version? 
2- What is projection buffer? 
3- How the projection matrix is calcualted?
4- Who is the person works with RabbitCT